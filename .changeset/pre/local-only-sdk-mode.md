---
'@ainotation/sdk': minor
---

Add `mcp: false` for local-only instances that skip saved MCP credentials, connections and synchronization while retaining annotations, image attachments, copying and exporting. Settings explains local-only operation in all five supported languages.
